You're given an array containing numbers from 1 to n, but two numbers are missing. Write a function that finds and returns the missing numbers.

findTwoMissingNumbers([1, 3, 5, 6], 6);
// [2, 4]

Numbers go from 1 to n, but now exactly two numbers are missing.


