class CoffeeShop {
	constructor (name, menu, orders) {
		this.name = name;
    this.menu = menu;
    this.orders = orders;
	}
  addOrder {
    
  }

}

const 

document.getElementById('ans').textContent = CoffeeShop.