Write a function called countVowels that takes a string and returns how many vowels (a, e, i, o, u) are in it.

countVowels("hello");       // 2
countVowels("javascript");  // 3
countVowels("rhythm");      // 0
countVowels("AEIOU");       // 5

Requirements: It should work with both uppercase and lowercase letters. 