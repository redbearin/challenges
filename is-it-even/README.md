Write a function called isEven that takes a number and returns:

true if the number is even
false if the number is odd

Example:

isEven(8);  // true
isEven(13); // false
isEven(0);  // true

Rules: Try to solve it using an if statement and the % (remainder) operator.

