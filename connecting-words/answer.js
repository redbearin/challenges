join = arr => {
  let idx;
  let str = '';
  let word1, word2;
  for (let i = 1; i < arr.length; i++) {
    word1 = arr[i-1];
    word2 = arr[i];
    count = 1;
    for (let j = 0; j < arr[i].length; j++) {
      
    }
  }
}

const arr = ["move", "over", "very"];

document.getElementById('ans').textContent = join(arr);